{
  "name": "PrimeImage",
  "version": "0.1.0",
  "description": "Turn any image into an prime number.",
  "main": "main.js",
  "scripts": {
    "lint": "eslint .",
    "test": "elm test",
    "build": "elm make elm-src/Main.elm --output site/built/elm.js",
    "serve": "bundle exec jekyll serve --source site"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-test": "^0.18.12"
  }
}

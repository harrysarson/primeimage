{
  "name": "PrimeImage",
  "version": "0.0.1",
  "description": "Turn any image into an prime number.",
  "main": "main.js",
  "scripts": {
    "lint": "eslint .",
    "test": "elm test",
    "build": "elm make elm-src/Main.elm --output compiled-elm/elm.js"
  },
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-test": "^0.18.12"
  }
}
